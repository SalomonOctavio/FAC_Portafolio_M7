package cl.portafolio.m7.notificaciones.web.dto;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

// Ajusta los nombres de clases a las que tengas en este package:
class DtosWebSmokeTest {

    @Test
    void construccionBasica() {
        // Ejemplos; reemplaza por tus clases reales:
        // var req = new NotificacionRequest("Asunto", "dest@test.com", "Mensaje", false);
        // assertEquals("Asunto", req.asunto());
        assertTrue(true); // placeholder si aún no sabes el nombre exacto
    }
}
